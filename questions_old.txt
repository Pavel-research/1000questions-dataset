!github.yaml
users = SELECT(User)
issues assigned to Mike = SELECT(Issue)=>FILTER(assigned_to,'Mike','in')
issues created by Mike = SELECT(Issue)=>FILTER(created_by,'Mike','in')
issues created by Mike or Gleb = SELECT(Issue)=>FILTER(created_by,set('Mike','Gleb'),'in')
repositories with issues created by Mike = SELECT(Repository)=>FILTER_RELATED(INVERSE_VALUES(created_by,SELECT(Issue)=>FILTER(created_by,'Mike','in'))
open issues=SELECT(Issue)=>FILTER_RELATED('open')
number of open issues=SELECT(Issue)=>COUNT(FILTER_RELATED('open'))
issues with more then 5 comments=SELECT(Issue)=>FILTER(comments,"5",">")
issues with more comments then in #434 =SELECT(Issue)=>FILTER(comments,PROPERTY("#434",comments),">")
repositories with more issues then Denis created = SELECT(Repository)=>FILTER(issues,PROPERTY("Denis",created),"count>")
Who created more issues then Denis = SELECT(User)=>FILTER(issues,PROPERTY("Denis",created),"count>")
comments created by users from CISCO = SELECT(Comment)=>FILTER(user,SELECT(User)=>FILTER(from,"CISCO","in"),'in')

comments created by Mike in #434 = SELECT(Comment)=>FILTER(issues,'#434','in')=>FILTER(created_by,'Mike','in')
comments created by Mike or Gleb in #434 = SELECT(Comment)=>FILTER(issues,'#434','in')=>FILTER(created_by,set('Mike','Gleb'),'in')
comments in issues created by Mike = SELECT(Comment)=>FILTER(issues,PROPERTY('Mike',created),'in')
comments created by Gleb in issues created by Mike = SELECT(Comment)=>FILTER_RELATED(SELECT(Issue)=>FILTER(created_by,'Mike','in'))=>FILTER(created_by,'Gleb','in')
comments in issues assigned to Mike = SELECT(Comment)=>FILTER(issues,PROPERTY('Mike',assigned_to),'in')
comments in issues assigned to Mike or Gleb = SELECT(Comment)=>FILTER(issues,PROPERTY(set('Mike','Gleb'),assigned_to),'in')
comments in issues assigned to Mike and Gleb = SELECT(Comment)=>FILTER(issues,PROPERTY(and('Mike','Gleb'),assigned_to),'in')
comments in issues not assigned to Mike or Gleb = SELECT(Comment)=>FILTER(issues,PROPERTY(not(or('Mike','Gleb')),assigned_to),'in')
comments in issues not assigned to Mike and Gleb = SELECT(Comment)=>FILTER(issues,PROPERTY(not(and('Mike','Gleb')),assigned_to),'in')
comments in issues closed by Mike = SELECT(Comment)=>FILTER(issues,PROPERTY('Mike',closed_by),'in')
comments created by Mike in issues closed by Gleb = SELECT(Comment)=>FILTER_RELATED(SELECT(Issue)=>FILTER(closed_by,'Gleb','in'))=>FILTER(created_by,'Mike','in')
comments in issues created by users from Com = SELECT(Comment)=>FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in')
comments created by Gleb in issues created by users from Com = SELECT(Comment)=>FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in')=>FILTER(created_by,'Gleb','in')
comments in issues with label "Bug" = SELECT(Comment)=>FILTER(issues,PROPERTY('Bug',labels),'in')
comments in issues with label "Bug" or "ML" = SELECT(Comment)=>FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in')
comments in issues with labels "Bug" and "ML" = SELECT(Comment)=>FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in')
comments created by Mike in issues with label "Bug" = SELECT(Comment)=>FILTER(issues,PROPERTY('Bug',labels),'in')=>FILTER(created_by,'Mike','in')
comments created by Mike in issues with label "Bug" or "ML" = SELECT(Comment)=>FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in')=>FILTER(created_by,'Mike','in')
comments created by Mike in issues with labels "Bug" and "ML" = SELECT(Comment)=>FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in')=>FILTER(created_by,'Mike','in')
comments created by Mike or Gleb in issues with label "Bug" = SELECT(Comment)=>FILTER(issues,PROPERTY('Bug',labels),'in')=>FILTER(created_by,or('Mike','Gleb'),'in')
comments created by Mike or Gleb in issues with label "Bug" or "ML" = SELECT(Comment)=>FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in')=>FILTER(created_by,or('Mike','Gleb'),'in')
comments created by Mike or Gleb in issues with labels "Bug" and "ML" = SELECT(Comment)=>FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in')=>FILTER(created_by,or('Mike','Gleb'),'in')
comments created by users from Com in issues with label "Bug" = SELECT(Comment)=>FILTER(user,PROPERTY('Com',from),'in')=>FILTER(issues,PROPERTY('Bug',labels),'in')
comments created by users from Com in issues with label "Bug" or "ML" = SELECT(Comment)=>FILTER(user,PROPERTY('Com',from),'in')=>FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in')
comments created by users from Com in issues with labels "Bug" and "ML" = SELECT(Comment)=>FILTER(user,PROPERTY('Com',from),'in')=>FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in')
comments in issues with label "Bug" created by Mike = SELECT(Comment)=>FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY('Mike', created)),'in')
comments in issues with label "Bug" or "ML" created by Mike = SELECT(Comment)=>FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY('Mike', created)),'in')
comments in issues with labels "Bug" and "ML" created by Mike = SELECT(Comment)=>FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY('Mike', created)),'in')
comments in issues with label "Bug" created by Mike or Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(or('Mike','Gleb'), created)),'in')
comments in issues with label "Bug" or "ML" created by Mike or Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(or('Mike','Gleb'), created)),'in')
comments in issues with labels "Bug" and "ML" created by Mike or Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(or('Mike','Gleb'), created)),'in')
comments in issues with label "Bug" created by users from Com = SELECT(Comment)=>FILTER(issues,PROPERTY('Bug',labels),'in')=>FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in')
comments in issues with label "Bug" or "ML" created by users from Com = SELECT(Comment)=>FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in')=>FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in')
comments in issues with labels "Bug" and "ML" created by users from Com = SELECT(Comment)=>FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in')=>FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in')
comments in issues with label "Bug" assigned to Mike = SELECT(Comment)=>FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY('Mike', assigned_to)),'in')
comments in issues with label "Bug" or "ML" assigned to Mike = SELECT(Comment)=>FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY('Mike', assigned_to)),'in')
comments in issues with labels "Bug" and "ML" assigned to Mike = SELECT(Comment)=>FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY('Mike', assigned_to)),'in')
comments in issues with label "Bug" assigned to Mike or Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(or('Mike','Gleb'), assigned_to)),'in')
comments in issues with label "Bug" or "ML" assigned to Mike or Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(or('Mike','Gleb'), assigned_to)),'in')
comments in issues with labels "Bug" and "ML" assigned to Mike or Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(or('Mike','Gleb'), assigned_to)),'in')
comments in issues with label "Bug" assigned to Mike and Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(and('Mike','Gleb'), assigned_to)),'in')
comments in issues with label "Bug" or "ML" assigned to Mike and Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(and('Mike','Gleb'), assigned_to)),'in')
comments in issues with labels "Bug" and "ML" assigned to Mike and Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(and('Mike','Gleb'), assigned_to)),'in')
comments in issues with label "Bug" not assigned to Mike or Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(or(not('Mike'),not('Gleb')), assigned_to)),'in')
comments in issues with label "Bug" or "ML" not assigned to Mike or Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(or(not('Mike'),not('Gleb')), assigned_to)),'in')
comments in issues with labels "Bug" and "ML" not assigned to Mike or Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(or(not('Mike'),not('Gleb')), assigned_to)),'in')
comments in issues with label "Bug" not assigned to Mike and Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(not(and('Mike','Gleb')), assigned_to)),'in')
comments in issues with label "Bug" or "ML" not assigned to Mike and Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(not(and('Mike','Gleb')), assigned_to)),'in')
comments in issues with labels "Bug" and "ML" not assigned to Mike and Gleb = SELECT(Comment)=>FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(not(and('Mike','Gleb')), assigned_to)),'in')
number of comments = COUNT(SELECT(Comment))
number of comments created by Mike = SELECT(Comment)=>COUNT(FILTER(created_by,'Mike','in'))
number of comments created by Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(created_by,set('Mike','Gleb'),'in'))
number of comments created by users from Com = SELECT(Comment)=>COUNT(FILTER(user,PROPERTY('Com',from),'in'))
number of comments in #434 = SELECT(Comment)=>COUNT(FILTER(issues,'#434','in'))
number of comments created by Mike in issue #434 = SELECT(Comment)=>COUNT(FILTER(issues,'#434','in')=>FILTER(created_by,'Mike','in'))
number of comments created by Mike or Gleb in issue #434 = SELECT(Comment)=>COUNT(FILTER(issues,'#434','in')=>FILTER(created_by,set('Mike','Gleb'),'in'))
number of comments in issues assigned to Mike = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY('Mike',assigned_to),'in'))
number of comments in issues assigned to Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(set('Mike','Gleb'),assigned_to),'in'))
number of comments in issues assigned to Mike and Gleb = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(and('Mike','Gleb'),assigned_to),'in'))
number of comments in issues not assigned to Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(not(or('Mike','Gleb')),assigned_to),'in'))
number of comments in issues not assigned to Mike and Gleb = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(not(and('Mike','Gleb')),assigned_to),'in'))
number of comments in issues created by Mike = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY('Mike',created),'in'))
number of comments created by Gleb in issues created by Mike = SELECT(Comment)=>COUNT(FILTER_RELATED(SELECT(Issue)=>FILTER(created_by,'Mike','in'))=>FILTER(created_by,'Gleb','in'))
number of comments in issues closed by Mike = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY('Mike',closed_by),'in'))
number of comments created by Gleb in issues closed by Mike = SELECT(Comment)=>COUNT(FILTER_RELATED(SELECT(Issue)=>FILTER(closed_by,'Gleb','in'))=>FILTER(created_by,'Mike','in'))
number of comments in issues created by users from Com = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in'))
number of comments created by Gleb in issues created by users from Com = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in')=>FILTER(created_by,'Gleb','in'))
number of comments in issues with label "Bug" = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY('Bug',labels),'in'))
number of comments in issues with label "Bug" or "ML" = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in'))
number of comments in issues with labels "Bug" and "ML" = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in'))
number of comments created by Mike in issues with label "Bug" = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY('Bug',labels),'in')=>FILTER(created_by,'Mike','in'))
number of comments created by Mike in issues with label "Bug" or "ML" = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in')=>FILTER(created_by,'Mike','in'))
number of comments created by Mike in issues with labels "Bug" and "ML" = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in')=>FILTER(created_by,'Mike','in'))
number of comments created by Mike or Gleb in issues with label "Bug" = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY('Bug',labels),'in')=>FILTER(created_by,or('Mike','Gleb'),'in'))
number of comments created by Mike or Gleb in issues with label "Bug" or "ML" = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in')=>FILTER(created_by,or('Mike','Gleb'),'in'))
number of comments created by Mike or Gleb in issues with labels "Bug" and "ML" = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in')=>FILTER(created_by,or('Mike','Gleb'),'in'))
number of comments created by users from Com in issues with label "Bug" = SELECT(Comment)=>COUNT(FILTER(user,PROPERTY('Com',from),'in')=>FILTER(issues,PROPERTY('Bug',labels),'in'))
number of comments created by users from Com in issues with label "Bug" or "ML" = SELECT(Comment)=>COUNT(FILTER(user,PROPERTY('Com',from),'in')=>FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in'))
number of comments created by users from Com in issues with labels "Bug" and "ML" = SELECT(Comment)=>COUNT(FILTER(user,PROPERTY('Com',from),'in')=>FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in'))
number of comments in issues with label "Bug" created by Mike = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY('Mike', created)),'in'))
number of comments in issues with label "Bug" or "ML" created by Mike = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY('Mike', created)),'in'))
number of comments in issues with labels "Bug" and "ML" created by Mike = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY('Mike', created)),'in'))
number of comments in issues with label "Bug" created by Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(or('Mike','Gleb'), created)),'in'))
number of comments in issues with label "Bug" or "ML" created by Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(or('Mike','Gleb'), created)),'in'))
number of comments in issues with labels "Bug" and "ML" created by Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(or('Mike','Gleb'), created)),'in'))
number of comments in issues with label "Bug" created by users from Com = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY('Bug',labels),'in')=>FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in'))
number of comments in issues with label "Bug" or "ML" created by users from Com = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(or('Bug', 'ML'),labels),'in')=>FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in'))
number of comments in issues with labels "Bug" and "ML" created by users from Com = SELECT(Comment)=>COUNT(FILTER(issues,PROPERTY(and('Bug', 'ML'),labels),'in')=>FILTER(issues,PROPERTY(PROPERTY('Com', from), created_by),'in'))
number of comments in issues with label "Bug" assigned to Mike = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY('Mike', assigned_to)),'in'))
number of comments in issues with label "Bug" or "ML" assigned to Mike = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY('Mike', assigned_to)),'in'))
number of comments in issues with labels "Bug" and "ML" assigned to Mike = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY('Mike', assigned_to)),'in'))
number of comments in issues with label "Bug" assigned to Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(or('Mike','Gleb'), assigned_to)),'in'))
number of comments in issues with label "Bug" or "ML" assigned to Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(or('Mike','Gleb'), assigned_to)),'in'))
number of comments in issues with labels "Bug" and "ML" assigned to Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(or('Mike','Gleb'), assigned_to)),'in'))
number of comments in issues with label "Bug" assigned to Mike and Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(and('Mike','Gleb'), assigned_to)),'in'))
number of comments in issues with label "Bug" or "ML" assigned to Mike and Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(and('Mike','Gleb'), assigned_to)),'in'))
number of comments in issues with labels "Bug" and "ML" assigned to Mike and Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(and('Mike','Gleb'), assigned_to)),'in'))
number of comments in issues with label "Bug" not assigned to Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(or(not('Mike'),not('Gleb')), assigned_to)),'in'))
number of comments in issues with label "Bug" or "ML" not assigned to Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(or(not('Mike'),not('Gleb')), assigned_to)),'in'))
number of comments in issues with labels "Bug" and "ML" not assigned to Mike or Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(or(not('Mike'),not('Gleb')), assigned_to)),'in'))
number of comments in issues with label "Bug" not assigned to Mike and Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY('Bug', labels), PROPERTY(not(and('Mike','Gleb')), assigned_to)),'in'))
number of comments in issues with label "Bug" or "ML" not assigned to Mike and Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(or('Bug','ML'), labels), PROPERTY(not(and('Mike','Gleb')), assigned_to)),'in'))
number of comments in issues with labels "Bug" and "ML" not assigned to Mike and Gleb = SELECT(Comment)=>COUNT(FILTER(issues,and(PROPERTY(and('Bug','ML'), labels), PROPERTY(not(and('Mike','Gleb')), assigned_to)),'in'))
Who created comments in #434 = SELECT(User)=>FILTER(comments,PROPERTY('#434',comments),'in')
Who created comments in issue #434 = SELECT(User)=>FILTER(comments,PROPERTY('#434',comments),'in')
Who created comments in issues created by Mike = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, PROPERTY('Mike', created),'in'),comments),'in')
Who created comments in issues assigned to Mike = SELECT(User)=>FILTER(comments, PROPERTY(FILTER(issues, PROPERTY('Mike', assigned_to), 'in'), comments),'in')
Who created comments in issues assigned to Mike or Gleb = SELECT(User)=>FILTER(comments, PROPERTY(FILTER(issues, PROPERTY(or('Mike','Gleb'), assigned_to), 'in'), comments),'in')
Who created comments in issues assigned to Mike and Gleb = SELECT(User)=>FILTER(comments, PROPERTY(FILTER(issues, PROPERTY(and('Mike','Gleb'), assigned_to), 'in'), comments),'in')
Who created comments in issues not assigned to Mike or Gleb = SELECT(User)=>FILTER(comments, PROPERTY(FILTER(issues, PROPERTY(or(not('Mike'),not('Gleb')), assigned_to), 'in'), comments),'in')
Who created comments in issues not assigned to Mike and Gleb = SELECT(User)=>FILTER(comments, PROPERTY(FILTER(issues, PROPERTY(not(and('Mike','Gleb')), assigned_to), 'in'), comments),'in')
Who created comments in issues closed by Mike = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, PROPERTY('Mike', closed_by),'in'),comments),'in')
Who created comments in issues with label "Bug" = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues,PROPERTY('Bug',labels),'in'),comments),'in')
Who created comments in issues with label "Bug" or "ML" = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues,PROPERTY(or('Bug','ML'),labels),'in'),comments),'in')
Who created comments in issues with labels "Bug" and "ML" = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues,PROPERTY(and('Bug','ML'),labels),'in'),comments),'in')
Who created comments in issues with label "Bug" created by Mike = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY('Bug',labels),PROPERTY('Mike', created)),'in'),comments),'in')
Who created comments in issues with label "Bug" or "ML" created by Mike = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(or('Bug','ML'),labels),PROPERTY('Mike', created)),'in'),comments),'in')
Who created comments in issues with labels "Bug" and "ML" created by Mike = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(and('Bug','ML'),labels),PROPERTY('Mike', created)),'in'),comments),'in')
Who created comments in issues with label "Bug" created by Mike or Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY('Bug',labels),PROPERTY(or('Mike','Gleb'), created)),'in'),comments),'in')
Who created comments in issues with label "Bug" or "ML" created by Mike or Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(or('Bug','ML'),labels),PROPERTY(or('Mike','Gleb'), created)),'in'),comments),'in')
Who created comments in issues with labels "Bug" and "ML" created by Mike or Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(and('Bug','ML'),labels),PROPERTY(or('Mike','Gleb'), created)),'in'),comments),'in')
Who created comments in issues with label "Bug" assigned to Mike = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY('Bug',labels),PROPERTY('Mike', assigned_to)),'in'),comments),'in')
Who created comments in issues with label "Bug" or "ML" assigned to Mike = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(or('Bug','ML'),labels),PROPERTY('Mike', assigned_to)),'in'),comments),'in')
Who created comments in issues with labels "Bug" and "ML" assigned to Mike = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(and('Bug','ML'),labels),PROPERTY('Mike', assigned_to)),'in'),comments),'in')
Who created comments in issues with label "Bug" assigned to Mike or Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY('Bug',labels),PROPERTY(or('Mike','Gleb'), assigned_to)),'in'),comments),'in')
Who created comments in issues with label "Bug" or "ML" assigned to Mike or Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(or('Bug','ML'),labels),PROPERTY(or('Mike','Gleb'), assigned_to)),'in'),comments),'in')
Who created comments in issues with labels "Bug" and "ML" assigned to Mike or Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(and('Bug','ML'),labels),PROPERTY(or('Mike','Gleb'), assigned_to)),'in'),comments),'in')
Who created comments in issues with label "Bug" assigned to Mike and Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY('Bug',labels),PROPERTY(and('Mike','Gleb'), assigned_to)),'in'),comments),'in')
Who created comments in issues with label "Bug" or "ML" assigned to Mike and Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(or('Bug','ML'),labels),PROPERTY(and('Mike','Gleb'), assigned_to)),'in'),comments),'in')
Who created comments in issues with labels "Bug" and "ML" assigned to Mike and Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(and('Bug','ML'),labels),PROPERTY(and('Mike','Gleb'), assigned_to)),'in'),comments),'in')
Who created comments in issues with label "Bug" not assigned to Mike or Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY('Bug',labels),PROPERTY(or(not('Mike'),not('Gleb')), assigned_to)),'in'),comments),'in')
Who created comments in issues with label "Bug" or "ML" not assigned to Mike or Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(or('Bug','ML'),labels),PROPERTY(or(not('Mike'),not('Gleb')), assigned_to)),'in'),comments),'in')
Who created comments in issues with labels "Bug" and "ML" not assigned to Mike or Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(and('Bug','ML'),labels),PROPERTY(or(not('Mike'),not('Gleb')), assigned_to)),'in'),comments),'in')
Who created comments in issues with label "Bug" not assigned to Mike and Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY('Bug',labels),PROPERTY(not(and('Mike','Gleb')), assigned_to)),'in'),comments),'in')
Who created comments in issues with label "Bug" or "ML" not assigned to Mike and Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(or('Bug','ML'),labels),PROPERTY(not(and('Mike','Gleb')), assigned_to)),'in'),comments),'in')
Who created comments in issues with labels "Bug" and "ML" not assigned to Mike and Gleb = SELECT(User)=>FILTER(comments,PROPERTY(FILTER(issues, and(PROPERTY(and('Bug','ML'),labels),PROPERTY(not(and('Mike','Gleb')), assigned_to)),'in'),comments),'in')